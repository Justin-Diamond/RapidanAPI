import pandas as pd
import pytest

def test_dataframe_creation():
    """Test to ensure a pandas DataFrame can be created and has the correct shape."""
    data = {'col1': [1, 2], 'col2': [3, 4]}
    df = pd.DataFrame(data)
    assert df.shape == (2, 2)
